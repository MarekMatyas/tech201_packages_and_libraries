# pip and packages

# pip is Python's package manager and installer (anything external we have to use pip).
#
# import requests
#
# request_bbc = requests.get("https://www.google.co.uk") # we can use this to get function from URL
#
# print(request_bbc.status_code)  # output is 200 which mean everything is fine
#
# print(request_bbc.content)  ## get all the entire content on that webpage



